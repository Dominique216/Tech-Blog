 {{!--need to to an each loop for each of the individual users posts  --}}
 {{#each posts as |post|}}
 {{!-- <div class="card m-5">
    <div class="card-header">
        {{post.title}}<span class="float-right">{{user.name}} {{post.date_created}}</span>
    </div>
    <div class="card-body">
        {{post.content}}
    </div>    
</div> --}}
        <div class="text-center">
            <button type="button" class="btn btn-secondary btn-lg w-75 mt-5 post-btn" data-type="{{post.id}}">{{post.title}}</button>
        </div>
  {{/each}}
<div class="text-center">
    <button id="add-post"type="button" class="btn btn-outline-success btn-lg my-5 w-75">Add Post</button>
</div> 

<script src="/js/dashboard.js"></script>